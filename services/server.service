[Unit]
Description=User interface server
Wants=basic.target
After=basic.target network.target

[Service]
SyslogIdentifier=backend
StandardOutput=syslog
StandardError=syslog
ExecStart=/usr/local/bin/bookpi-backend
Restart=always

[Install]
WantedBy=multi-user.target
